{% extends 'base.html.twig' %}

{% block title %}{{ folder.name }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/library_style.css') }}">
{% endblock stylesheets %}

{% block folder %}{% endblock folder %}
{# {% block add_document %}{{ path('app_document_new', {slug_folder: folder.slug}) }}{% endblock add_document %} #}

{% block features %}
    
{% endblock features %}
    {% block data_show %}
    {# <div class="search">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
    {% if documents|length > 0 %}
        <div class="data-folder">   
            <ul>
                <h1><li class="data-list">{{ folder.name }} :</li></h1>

            {% for document in documents %}

                <li class="data-list">
                    <a href="{{ path('app_document_show', {slug_folder: folder.slug, slug: document.slug}) }}">{{ document.name }}</a>
                    <a href="{{ path('document_download', {'slug': document.slug}) }}">download</a>
                    {{ include('document/_delete_form.html.twig') }}
                </li>

            {% endfor %}

            </ul>
        </div>
        {% else %}
            <p>Aucun document trouvé dans ce dossier.</p>
        {% endif %} #}


    <div class="container">
        <div class="text-right pb-2 pt-2 btn-box">
            <button class="btn btn-secondary">
                <a href="{{ path('app_library_show', {'slug': library.slug}) }}"> < back</a>
            </button>
            <button class="btn btn-primary">
                <a href="{{ path('app_document_new', {slug_folder: folder.slug}) }}">Create new</a>
            </button>
        </div>
    
    
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">{{ folder.name }}</th>
                    <th scope="col" class="text-right" >Action</th>
                </tr>
            </thead>
            <tbody>
                {% for document in documents %}
                <tr>
                    <th scope="row" class="th-name"><a href="{{ path('app_document_show', {slug_folder: folder.slug, slug: document.slug}) }}">{{ document.name }}</a></th>
                    <td class="text-right">
                        {# <button class="btn btn-primary btn-rud">Supprimer</button>
                        <button class="btn btn-secondary btn-rud">Modifier</button>
                        <button class="btn btn-primary btn-rud"><a href="{{ path('app_document_show', {slug_folder: folder.slug, slug: document.slug}) }}">Voir</a></button> #}
                        <button type="button" class="btn btn-primary btn-rud" data-toggle="modal" data-target="#Modal-{{document.slug}}">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                                class="bi bi-gear-fill" viewBox="0 0 16 16">
                                <path
                                    d="M9.405 1.05c-.413-1.4-2.397-1.4-2.81 0l-.1.34a1.464 1.464 0 0 1-2.105.872l-.31-.17c-1.283-.698-2.686.705-1.987 1.987l.169.311c.446.82.023 1.841-.872 2.105l-.34.1c-1.4.413-1.4 2.397 0 2.81l.34.1a1.464 1.464 0 0 1 .872 2.105l-.17.31c-.698 1.283.705 2.686 1.987 1.987l.311-.169a1.464 1.464 0 0 1 2.105.872l.1.34c.413 1.4 2.397 1.4 2.81 0l.1-.34a1.464 1.464 0 0 1 2.105-.872l.31.17c1.283.698 2.686-.705 1.987-1.987l-.169-.311a1.464 1.464 0 0 1 .872-2.105l.34-.1c1.4-.413 1.4-2.397 0-2.81l-.34-.1a1.464 1.464 0 0 1-.872-2.105l.17-.31c.698-1.283-.705-2.686-1.987-1.987l-.311.169a1.464 1.464 0 0 1-2.105-.872l-.1-.34zM8 10.93a2.929 2.929 0 1 1 0-5.86 2.929 2.929 0 0 1 0 5.858z" />
                            </svg>
                        </button>
                        
                    <!-- Modal -->
                    <div class="modal fade" id="Modal-{{document.slug}}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title text-center" id="exampleModalLabel">{{document.name}} - parameter</h5>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                </div>
                                <div class="modal-body">
                                    {{ include('document/_delete_form.html.twig') }}
                                    <button class="btn btn-secondary btn-rud">
                                        <a href="{{ path('app_document_edit', {slug_folder: folder.slug, slug: document.slug}) }}">Modifier</a>
                                    </button>
                                    <button class="btn btn-secondary btn-rud">
                                        <a href="{{ path('document_download', {'slug': document.slug}) }}">download</a>
                                    </button>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                </div>
                            </div>
                        </div>
                    </div>
                    </td>
                    {% else %}
                    <th>no records found</th>
    
                </tr>
                {% endfor %}
    
            </tbody>
        </table>
    </div>

    {% endblock data_show %}

