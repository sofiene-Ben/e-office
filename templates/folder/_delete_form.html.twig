<form method="post" action="{{ path('app_folder_delete',  {slug_lib: library.slug, slug: folder.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ folder.id) }}">
    <button class="btn">Delete</button>
</form>
