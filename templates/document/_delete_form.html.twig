<form method="post" action="{{ path('app_document_delete', {slug_folder: folder.slug, slug: document.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document.id) }}">
    <button class="btn btn-primary btn-rud">Delete</button>
</form>
